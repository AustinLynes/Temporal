#pragma once

#include <string>


#include <array>	// static array
#include <vector>	// dynamic array
#include <list>		// double link list

// QUEUES (FIFO)
#include <queue>
#include <deque>
// STACK (LIFO)
#include <stack>	
// USED FOR CREATING UNIQUE LISTS
#include <set>		// includes, set, multiset
#include <unordered_set> // unordered, includes u_set, u_multiset
// USED FOR CREATING UNIQUE KEY->VALUE PAIRING RELATION SHIPS
#include <map>		// includes, map, multimap
#include <unordered_map>// unordered, includes u_map, u_mulimap

// data structure funcs
#include <algorithm>

#include <type_traits>
